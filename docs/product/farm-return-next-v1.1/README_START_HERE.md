# Farm Return Next v1.1 — Claude-ready bundle

Place this whole folder inside the Farm Return repository at:

`docs/product/farm-return-next-v1.1/`

The important files are:

- `FARM_RETURN_NEXT_SPEC_v1_1.md` — Claude-readable source of truth
- `media/` — visual references extracted from the approved Word brief
- `OVERNIGHT_BUILD_PROMPT.md` — paste/run this in Claude Code for the overnight build
- `Farm_Return_Next_Product_UX_Specification_v1_1.docx` — human-readable master copy

Before starting the overnight run, confirm the branch is `farm-return-next`, the Mac is plugged in and will not sleep, and both `claude` and `codex` are authenticated.
