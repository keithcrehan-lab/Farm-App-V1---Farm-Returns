{
  "schema_version": 1,
  "programme": "farm-return-next",
  "baseline_tag": "v1-baseline-2026-08-29",
  "baseline_sha": "9c8a952b77227ddfbd44c7efabf8e5bdd06c77f4",
  "branch": "farm-return-next-checkpoint2-jobs-persistence-revised",
  "current_checkpoint": "checkpoint-2-vertical-d-decisions-jobs-persistence",
  "checkpoint_status": "complete_pending_product_owner_merge_decision",
  "contracts_frozen": true,
  "last_quality_gate": {
    "run_at": "2026-08-31T21:24:00Z",
    "result": "pass",
    "tests": "1075/1075 pass (78/78 files)",
    "typecheck": "pass",
    "lint": "pass",
    "build": "pass (26 routes in the build output's route table -- includes /today; no route added or removed by this revision)",
    "note": "Checkpoint 2, Vertical D -- real decisions/jobs persistence, REVISED by a dedicated architectural security review after the original branch (farm-return-next-checkpoint2-jobs-persistence, 4645d70) escalated to a service-role client. See BLOCKERS.md's 'Decisions/jobs persistence: service-role reverted to RLS' entry and IMPLEMENTATION_LOG.md's matching section for the complete account. New/changed vs. farm-return-next: src/lib/farm-data/decisions.ts, src/lib/farm-data/jobs.ts (both now insert via the plain RLS-respecting session client, not a privileged one -- src/lib/supabase/service-role.ts does not exist on this branch), supabase/migrations/20260829010000_decisions_jobs_client_access.sql (grants select+insert to authenticated, not select-only). actRecordWeightObservation (src/orchestration/act/index.ts) persists a real Decision and Job alongside the existing WeightObservation write, via persistRecordWeightObservationAuditTrail, verified against a real WeightObservation via individual-animals.ts's getWeightObservationById -- unchanged by the reversal. Real tests: decisions.test.ts/jobs.test.ts (mock @/lib/supabase/server only; include a source-text assertion, on the file's real import statements, that no privileged client is imported), plus mappers.test.ts/act/index.test.ts extensions. Full test/typecheck/lint/build run on farm-return-next-checkpoint2-jobs-persistence-revised after a leftover subagent worktree (.claude/worktrees/agent-a784a558509e17e9a, the disputed branch's own checkout) was removed -- it had been producing false ESLint failures via .next/types/* generated files, the same pattern seen twice before in this build programme."
  },
  "last_codex_audit": {
    "run_at": "2026-08-29T20:33:10Z",
    "target": "--uncommitted (Checkpoint 2, Vertical D's diff, round 12 -- final round run)",
    "result": "one_finding_deliberately_deferred_per_project_policy_disputed_by_audit_tool_three_times_zero_new_facts",
    "critical_findings": 0,
    "high_findings": 1,
    "medium_findings": 1,
    "log_path": "docs/farm-return-next/audit-logs/20260829T203310Z.md (gitignored, ephemeral -- see IMPLEMENTATION_LOG.md for the durable round-by-round record)",
    "note": "Twelve real Codex audit rounds. Round 10 first found the one real open HIGH -- neither decisions nor jobs persists a reference to the real WeightObservation row a job actually produced -- investigated in depth and deferred, not fixed: it is the same 'Actuals aren't a queryable concept yet' gap this branch's own pre-existing estimate_calibration BLOCKERS.md entry already named (gating Vertical F on Vertical D -- this checkpoint -- existing first), and the same 'no agreed target-entity kind convention yet' gap the pre-existing jobs target-entity entry already named (Checkpoint 1, deferred to Vertical C). Rounds 11 and 12 restated the identical finding with zero new facts each time (round 12's own wording: 'The issue is acknowledged in BLOCKERS.md, but remains present in this diff' -- true of any deferred finding by definition). Three consecutive rounds producing no new facts is treated as real confirmation, matching this programme's own established precedent (Checkpoint 2, Vertical B's second slice) for when to stop. Round 11 also found one real, cheap LOW (a stale doc-comment retry example), fixed. Round 12 also found one new, real MEDIUM (auditTrailError has no real consumer in src/ yet -- true, and exactly the explicitly out-of-scope Records/Activity UI this task's own brief named), logged not fixed. Every other round's finding (real CRITICALs at rounds 1/3/4/5(x2)/6, real HIGHs throughout rounds 1-9) was fixed for real and re-verified, not deferred -- full round-by-round account in IMPLEMENTATION_LOG.md."
  },
  "last_codex_automation_smoke_test": {
    "run_at": "2026-08-28T23:56:10Z",
    "result": "pass",
    "detail": "scripts/codex-audit.sh --smoke-test: real `codex exec --sandbox read-only` call, authenticated (ChatGPT auth), model gpt-5.6-sol, returned CODEX_SMOKE_TEST_OK, 4784 tokens. Log: docs/farm-return-next/audit-logs/20260828T235610Z.md (gitignored, ephemeral)."
  },
  "last_claude_automation_smoke_test": {
    "run_at": "2026-08-28T23:56:11Z",
    "result": "pass",
    "detail": "scripts/autopilot.sh --smoke-test: real headless `claude -p` call returned CLAUDE_SMOKE_TEST_OK. Log: docs/farm-return-next/autopilot-logs/smoke-20260828T235611Z.log (gitignored, ephemeral)."
  },
  "last_autopilot_real_run": {
    "run_at": "2026-08-29T01:14:00Z",
    "iterations_requested": 1,
    "result": "stopped_by_bug_in_codex-audit.sh_then_manually_completed",
    "detail": "scripts/autopilot.sh (no flags, 1 iteration, no --auto-push), run by the product owner under caffeinate -dimsu. Headless Claude's orchestration-skeleton work + scripts/quality-gate.sh both succeeded for real; scripts/codex-audit.sh --uncommitted then failed on a genuine CLI-argument-incompatibility bug in the script itself (codex review rejects combining --uncommitted with a custom prompt), so autopilot.sh correctly refused to commit anything. The session then fixed the script (switched to codex exec) and completed the rest of Checkpoint 1 by hand across twelve real audit rounds -- the automated loop itself was not re-invoked. See IMPLEMENTATION_LOG.md for the full account."
  },
  "open_blockers_ref": "docs/farm-return-next/BLOCKERS.md",
  "open_critical_high_findings": 0,
  "open_critical_high_findings_note": "For Checkpoint 2, Vertical D (real decisions/jobs persistence): 0, by this project's own documented deferral policy -- deliberately DIFFERENT from round 10's own raw high_findings (1). The one real HIGH -- neither decisions nor jobs persists a reference to the real WeightObservation row a job actually produced -- is a genuine, evidenced gap, investigated in depth (not conceded, not rubber-stamped) and deferred because it is the same 'Actuals aren't a queryable concept yet' gap this branch's own pre-existing estimate_calibration BLOCKERS.md entry already named before this checkpoint started, explicitly gating Vertical F on Vertical D (this checkpoint) existing first -- and the same 'no agreed target-entity kind convention yet' gap the pre-existing jobs target-entity entry already named. A one-off fix scoped to only this one job type would itself be the premature, ungeneralized schema decision those two existing deferrals were already trying to avoid. See BLOCKERS.md's new dedicated entry (with its round-11/round-12 addenda) for the complete reasoning. Every other real Critical/High this slice's twelve audit rounds surfaced (rounds 1 and 3-9) was fixed for real and re-verified, not deferred. For the two PRIOR Vertical B slices (soil_test_age, spreading_window), this field's reasoning is unchanged from before this checkpoint -- see IMPLEMENTATION_LOG.md's own entries for those slices.",
  "next_action": "Checkpoint 2, Vertical D (real decisions/jobs persistence) is complete on farm-return-next-checkpoint2-jobs-persistence-revised, NOT YET MERGED into farm-return-next -- pending the product owner's own review of this revision, per the architectural security review's instruction ('do not merge the current service-role implementation yet'). actRecordWeightObservation (src/orchestration/act/index.ts) persists a real Decision and Job via persistRecordWeightObservationAuditTrail, backed by src/lib/farm-data/decisions.ts/jobs.ts, which insert via the plain RLS-respecting session client (no privileged/service-role client anywhere in this codebase). Migration supabase/migrations/20260829010000_decisions_jobs_client_access.sql is PENDING_DEV_VALIDATION -- needs the product owner's Supabase access to apply to Farm Return V1 Dev; no SUPABASE_SERVICE_ROLE_KEY or any other new env var is required. Real next steps once merged: (1) the Records/Activity UI extension BUILD_PLAN.md's Vertical D scope names is now unblocked (real decisions/jobs rows will exist once the migration is applied) but not built -- out of this task's scope, see BLOCKERS.md's dedicated entry for exactly what's available to read; (2) Vertical C (Act/Confirm/GPS job mode) needs its own real job-status-transition write path (jobs grants no update at all currently, deliberately) and its own target_type/target_id design for jobs, both untouched by this slice, both already documented in BLOCKERS.md from Checkpoint 1; (3) whoever designs a real 'Actual' concept for this schema (most likely Vertical F, needing it to reference confirmed Actuals for calibration) should treat round 10's deferred finding, and record_weight_observation as its first concrete example, as part of that same design -- documented in BLOCKERS.md, not resolved piecemeal; (4) the whole-application service-role question is now a genuinely open, undesigned question again (BLOCKERS.md's 'every other table' entry) -- there is no reusable service-role implementation in this codebase any more; whoever picks this up designs it fresh against a real, reviewed whole-app decision to do so, not by resurrecting the reverted pattern.",
  "notes": "BUILD_STATE.json is the single machine-readable source of truth scripts/autopilot.sh reads to decide what to do next. A human or agent updating BUILD_PLAN.md/IMPLEMENTATION_LOG.md must update this file in the same commit -- they must never drift apart."
}
