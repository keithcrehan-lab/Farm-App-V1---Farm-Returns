{
  "schema_version": 1,
  "programme": "farm-return-next",
  "baseline_tag": "v1-baseline-2026-08-29",
  "baseline_sha": "9c8a952b77227ddfbd44c7efabf8e5bdd06c77f4",
  "branch": "farm-return-next",
  "current_checkpoint": "checkpoint2-vertical-h-satellite-slice-1",
  "checkpoint_status": "round_1_remediation_applied_pending_reaudit",
  "contracts_frozen": true,
  "last_quality_gate": {
    "run_at": "2026-09-01T16:33:00Z",
    "result": "pass",
    "tests": "1223/1223 pass (89/89 files)",
    "typecheck": "pass",
    "lint": "pass",
    "build": "pass (26 routes, unchanged)",
    "note": "After round-1 remediation: real finiteness/range validation on parseStacFeature's numeric fields (bbox coords, cloud cover, statistics, datetime), asOf/lookbackDays validation in selectBestSatelliteCoverage. +10 tests over the initial 1213 figure."
  },
  "last_codex_audit": {
    "run_at": "2026-09-01T15:09:36Z",
    "target": "--commit HEAD (Checkpoint 2 Vertical G notifications increment, round 2 of N)",
    "result": "0_critical_0_high_1_medium_fixed_reaudit_pending",
    "critical_findings": 0,
    "high_findings": 0,
    "medium_findings": 1,
    "log_path": "docs/farm-return-next/audit-logs/20260901T150936Z.md (gitignored, ephemeral -- see IMPLEMENTATION_LOG.md for the durable round-by-round record)",
    "note": "Round 2 (self-referential): this file's own last_codex_audit still pointed at Vertical A's final round instead of Vertical G's round 1 -- fixed by this exact write. Round 1 itself (docs/farm-return-next/audit-logs/20260901T150232Z.md) found 0 Critical + 1 High (notifications_check_valid_transition rejected every transition into 'expired', including the notifications_expiry pg_cron job's own scheduled update -- fixed via a current_user role check) + 1 Medium (insert grant doesn't itself verify Prompt provenance -- documented as the same already-accepted decisions.ts-precedent systemic limitation, not patched). BUILD_PLAN.md's gate (0 Critical/High) was NOT yet met by round 1's own result (it had 1 High); it was met once round 1's High was fixed and round 2 confirmed 0/0/1/0 -- correcting an inaccurate 'met since round 1' claim a round-3 Codex audit (docs/farm-return-next/audit-logs/20260901T151128Z.md, MEDIUM) correctly caught in this field's own prior text. Vertical A's telemetry/outbox increment (4 rounds, final 0/0/0/0) remains the prior clean, complete checkpoint. This field is a pointer to the single most recent audit, not a full history; this file must never carry two `last_codex_audit` keys."
  },
  "last_codex_automation_smoke_test": {
    "run_at": "2026-08-31T20:43:01Z",
    "result": "pass",
    "detail": "scripts/codex-audit.sh --smoke-test, re-run at the start of the overnight build run to confirm the Claude -> Codex workflow before starting a long build cycle, per that run's own explicit instruction. codex-cli 0.150.1, authenticated (ChatGPT auth), model gpt-5.6-sol, returned CODEX_SMOKE_TEST_OK, 4784 tokens. codex exec --help/codex review --help inspected first to confirm scripts/codex-audit.sh's existing --sandbox read-only invocation still matches this CLI version's actual flag surface -- no script change needed. Log: docs/farm-return-next/audit-logs/20260831T204301Z.md (gitignored, ephemeral)."
  },
  "last_claude_automation_smoke_test": {
    "run_at": "2026-08-28T23:56:11Z",
    "result": "pass",
    "detail": "scripts/autopilot.sh --smoke-test: real headless `claude -p` call returned CLAUDE_SMOKE_TEST_OK. Log: docs/farm-return-next/autopilot-logs/smoke-20260828T235611Z.log (gitignored, ephemeral)."
  },
  "last_autopilot_real_run": {
    "run_at": "2026-08-29T01:14:00Z",
    "iterations_requested": 1,
    "result": "stopped_by_bug_in_codex-audit.sh_then_manually_completed",
    "detail": "scripts/autopilot.sh (no flags, 1 iteration, no --auto-push), run by the product owner under caffeinate -dimsu. Headless Claude's orchestration-skeleton work + scripts/quality-gate.sh both succeeded for real; scripts/codex-audit.sh --uncommitted then failed on a genuine CLI-argument-incompatibility bug in the script itself (codex review rejects combining --uncommitted with a custom prompt), so autopilot.sh correctly refused to commit anything. The session then fixed the script (switched to codex exec) and completed the rest of Checkpoint 1 by hand across twelve real audit rounds -- the automated loop itself was not re-invoked. See IMPLEMENTATION_LOG.md for the full account. The current overnight run (this file's last_codex_audit/last_codex_automation_smoke_test) continues manually/interactively rather than via this script, for the same reason Checkpoint 1's did: it needs judgment calls (triage, false-positive rejection, scope decisions) at each round, not a fixed script loop."
  },
  "open_blockers_ref": "docs/farm-return-next/BLOCKERS.md",
  "open_critical_high_findings": 0,
  "open_critical_high_findings_note": "Vertical H's satellite-discovery slice: round 1 found 2 High (both fixed -- untrusted numeric/date parsing in the STAC client, unvalidated asOf/lookbackDays in the domain selector); a round-2 Codex re-audit against the fix commit has not run yet as of this write (next step). Vertical G's notifications increment: gate met since round 2. Vertical A's telemetry/outbox increment: 0 across all 4 audit rounds (final round clean, 0/0/0/0). For Vertical D Records UI and Vertical B's fourth Prompt: genuinely 0 across all audit rounds. For the earlier Vertical D persistence-layer checkpoint (decisions/jobs writer): 0 by the documented deferral-policy route recorded in this file's git history (the held, product-owner-authorized systemic RLS-forgery CRITICAL) -- unchanged.",
  "migrations": {
    "status": "APPLIED_DEV, not yet VALIDATED_DEV",
    "detail": "20260829000000_orchestration_foundation.sql, 20260829010000_decisions_jobs_client_access.sql, and 20260829020000_jobs_weight_observation_reference.sql are all applied to Farm Return V1 Dev, confirmed live by the product owner 2026-09-01. Real User A/User B cross-tenant RLS validation (each migration's own checklist) has NOT yet been run and confirmed all-PASS -- a ready-to-run script exists, supabase/validation/decisions_jobs_rls_validation.sql (Supabase's own documented SET LOCAL role authenticated + request.jwt.claims RLS-testing technique, uses two of the product owner's own existing real farms, no new accounts, self-rolling-back). This build environment cannot run it itself (no working network path to the database, confirmed three separate times this session, most recently via a 135+ second hang plus two genuine zombie processes found and killed; separately, creating/authenticating as test accounts is a hard policy prohibition regardless of network access). Run it via the Supabase Dashboard SQL Editor (or psql/supabase db query as the postgres role) and confirm every line reads PASS before updating any migration's status line (and this field) to VALIDATED_DEV -- do not mark validated on the strength of 'migrations applied' alone, those are two different, both-real claims."
  },
  "next_action": "Committing round-1 remediation of Checkpoint 2 Vertical H's satellite-discovery slice (finiteness/range validation on STAC parsing + asOf/lookbackDays validation) and running a round-2 Codex re-audit next -- not yet run as of this write. Vertical G's notifications increment is complete and pushed (gate met since round 2). Vertical A's telemetry_events/offline-outbox increment is complete and pushed (4 real Codex audit rounds, final 0/0/0/0). Migrations: the three Checkpoint-2-persistence migrations are APPLIED_DEV, not yet VALIDATED_DEV -- run supabase/validation/decisions_jobs_rls_validation.sql (hardened through 4 real Codex audit rounds, all fixed) via the Supabase Dashboard SQL Editor or psql/supabase db query as postgres, confirm every line reads PASS, then update those three migrations' status lines and this file's `migrations` field to VALIDATED_DEV; the telemetry (20260901000000/20260901010000) and notifications (20260901020000) migrations are PENDING_DEV_VALIDATION separately and need the same real-database-access application this session cannot perform itself, plus their own pg_cron validation checklists. Checkpoints 2 Vertical D (Records UI) and Vertical B's fourth Prompt are both complete, audited to zero findings, and pushed. Per the locked build-priority order (BUILD_PLAN.md), Vertical A's real GPS-capture wiring, Vertical C's job-mode screens, and Vertical G's notification-centre UI are all deliberately scoped out (see ARCHITECTURE.md/BLOCKERS.md) -- each needs either a real UI trigger or Vertical E's approved visual reference, neither of which exists yet. Next real build-order item once G's audit is clean: Vertical H (Sentinel-2 field intelligence, build-priority #6), independent of the visual-reference blocker.",
  "notes": "BUILD_STATE.json is the single machine-readable source of truth scripts/autopilot.sh reads to decide what to do next. A human or agent updating BUILD_PLAN.md/IMPLEMENTATION_LOG.md must update this file in the same commit -- they must never drift apart. This file must never carry two keys with the same name (valid JSON, but the parser silently keeps only the last one) -- a real, self-inflicted bug found and fixed 2026-09-01, see IMPLEMENTATION_LOG.md."
}
