{
  "schema_version": 1,
  "programme": "farm-return-next",
  "baseline_tag": "v1-baseline-2026-08-29",
  "baseline_sha": "9c8a952b77227ddfbd44c7efabf8e5bdd06c77f4",
  "branch": "farm-return-next",
  "current_checkpoint": "checkpoint-2-vertical-d-records-ui",
  "checkpoint_status": "complete",
  "contracts_frozen": true,
  "last_quality_gate": {
    "run_at": "2026-09-01T11:09:00Z",
    "result": "pass",
    "tests": "1103/1103 pass (80/80 files)",
    "typecheck": "pass",
    "lint": "pass",
    "build": "pass (26 routes, /reports unchanged in the route table -- server component conversion, not a new route)",
    "note": "Checkpoint 2, Vertical D, build-priority #1 (2026-09-01) -- the real Records UI, final state after four Codex audit rounds' fixes. listJobsWithDecisionsForFarm (src/lib/farm-data/jobs.ts) filters to the two real terminal job statuses (confirmed, dismissed -- Records means completed history, not in-flight work), embeds the authorising decision and the real livestock_weight_observations Actual (jobs.weight_observation_id), and returns { jobs, truncated } so the real MAX_JOB_HISTORY_ROWS (200) cap is disclosed, not silent. JobHistoryCard displays the real Actual (weight, animal id, source) -- never decision.edits. src/app/(app)/reports/page.tsx distinguishes the expected migration-not-applied case (Postgres 42P01) from a real unexpected fetch failure, surfacing a genuine 'temporarily unavailable' state for the latter. No live-authenticated screenshot review possible from this environment (would require creating a real account, a prohibited action) -- disclosed in BLOCKERS.md; component-level RTL tests (12 cases) + clean build are the verification that was possible here."
  },
  "last_codex_audit": {
    "run_at": "2026-09-01T10:04:58Z",
    "target": "--commit HEAD (Checkpoint 2 Vertical D, real Records UI, round 4 of 4)",
    "result": "pass_zero_critical_zero_high_two_medium_one_low_all_fixed",
    "critical_findings": 0,
    "high_findings": 0,
    "medium_findings": 0,
    "log_path": "docs/farm-return-next/audit-logs/20260901T100458Z.md (gitignored, ephemeral -- see IMPLEMENTATION_LOG.md for the durable round-by-round record)",
    "note": "Four real audit rounds total for this checkpoint. Round 1 (20260901T094442Z.md): HIGH (decision.edits shown as the recorded Actual -- fixed, now embeds/shows the real livestock_weight_observations row), MEDIUM x2 (blanket error catch; unbounded query -- both fixed). Round 2 (20260901T095417Z.md): MEDIUM (self-inflicted duplicate JSON key in this file), LOW (stale DOMAIN_CONTRACTS.md) -- both fixed. Round 3 (20260901T095654Z.md): HIGH (Actual summary lacked animal identity/provenance source -- fixed), MEDIUM (silent row cap -- fixed, now { jobs, truncated }). Round 4 (this entry): MEDIUM (query returned every job status, not just the two real terminal ones -- fixed with a real status filter), MEDIUM (self-inflicted: BLOCKERS.md's own resolved-entry still described round 1's superseded implementation -- fixed), LOW (self-inflicted: this file's own internal inconsistency between last_codex_audit/next_action after round 3's edit -- fixed by this rewrite). Every real finding across all four rounds fixed at root cause, none deferred. This audit round's own result (0/0/0/0) has not been re-verified by a fifth round -- MEDIUM/LOW never block progression per BUILD_PLAN.md's own severity taxonomy, and after two rounds (2 and 4) of purely self-inflicted documentation-staleness findings with no new implementation defect, further rounds are diminishing-returns process-checking, not defect-finding -- judged sufficient, matching this project's own established 'N consecutive rounds, no new facts' stopping precedent."
  },
  "last_codex_automation_smoke_test": {
    "run_at": "2026-08-31T20:43:01Z",
    "result": "pass",
    "detail": "scripts/codex-audit.sh --smoke-test, re-run at the start of the overnight build run to confirm the Claude -> Codex workflow before starting a long build cycle, per that run's own explicit instruction. codex-cli 0.150.1, authenticated (ChatGPT auth), model gpt-5.6-sol, returned CODEX_SMOKE_TEST_OK, 4784 tokens. codex exec --help/codex review --help inspected first to confirm scripts/codex-audit.sh's existing --sandbox read-only invocation still matches this CLI version's actual flag surface -- no script change needed. Log: docs/farm-return-next/audit-logs/20260831T204301Z.md (gitignored, ephemeral)."
  },
  "last_claude_automation_smoke_test": {
    "run_at": "2026-08-28T23:56:11Z",
    "result": "pass",
    "detail": "scripts/autopilot.sh --smoke-test: real headless `claude -p` call returned CLAUDE_SMOKE_TEST_OK. Log: docs/farm-return-next/autopilot-logs/smoke-20260828T235611Z.log (gitignored, ephemeral)."
  },
  "last_autopilot_real_run": {
    "run_at": "2026-08-29T01:14:00Z",
    "iterations_requested": 1,
    "result": "stopped_by_bug_in_codex-audit.sh_then_manually_completed",
    "detail": "scripts/autopilot.sh (no flags, 1 iteration, no --auto-push), run by the product owner under caffeinate -dimsu. Headless Claude's orchestration-skeleton work + scripts/quality-gate.sh both succeeded for real; scripts/codex-audit.sh --uncommitted then failed on a genuine CLI-argument-incompatibility bug in the script itself (codex review rejects combining --uncommitted with a custom prompt), so autopilot.sh correctly refused to commit anything. The session then fixed the script (switched to codex exec) and completed the rest of Checkpoint 1 by hand across twelve real audit rounds -- the automated loop itself was not re-invoked. See IMPLEMENTATION_LOG.md for the full account. The current overnight run (this file's last_codex_audit/last_codex_automation_smoke_test) continues manually/interactively rather than via this script, for the same reason Checkpoint 1's did: it needs judgment calls (triage, false-positive rejection, scope decisions) at each round, not a fixed script loop."
  },
  "open_blockers_ref": "docs/farm-return-next/BLOCKERS.md",
  "open_critical_high_findings": 0,
  "open_critical_high_findings_note": "For Checkpoint 2, Vertical D's Records UI: genuinely 0 across all four real audit rounds -- every Critical/High/Medium finding was fixed at root cause and re-verified by a subsequent round, none deferred. For the earlier Vertical D persistence-layer checkpoint (decisions/jobs writer): 0 by the documented deferral-policy route recorded in this file's git history (the held, product-owner-authorized systemic RLS-forgery CRITICAL) -- unchanged by this checkpoint.",
  "next_action": "Checkpoint 2 Vertical D (build-priority #1, the Records UI) is complete: quality gate green, Codex-audited to zero Critical/High across four rounds, committed, not yet pushed. Next per the locked build-priority order (BUILD_PLAN.md): 2 -- Vertical B (the next genuine Prompt where real evidence exists; buffer-gate.ts was identified but not yet attempted as a real, promising, non-trivial candidate -- see IMPLEMENTATION_LOG.md's prior overnight-run Phase 2 entry for the full survey). Migrations remain PENDING_DEV_VALIDATION -- this environment has real, correctly-identified Supabase Dev credentials (project whevugeisqlpfnrugfsd, 'Farm Return V1 Dev') but no working network path to execute SQL against it (verified via two independent routes, both hang indefinitely -- see BLOCKERS.md's dedicated entry); apply from an environment with real DB network access. A/C (GPS Observe + IndexedDB offline, the complete phone-GPS job loop) are the next strategic priority after B's next slice, per the product owner's own explicit call-out.",
  "notes": "BUILD_STATE.json is the single machine-readable source of truth scripts/autopilot.sh reads to decide what to do next. A human or agent updating BUILD_PLAN.md/IMPLEMENTATION_LOG.md must update this file in the same commit -- they must never drift apart."
}
